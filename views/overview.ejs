  <!-- https://chatgpt.com/share/67a55531-6930-8009-951e-a51d161fe03f  -->

  <!DOCTYPE html>
  <html lang="en">
  <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Diary Overview</title>
      <link rel="stylesheet" href="styles.css">
  </head>
  <body>
      <div class="container">
          <h1>Diary Overview</h1>
          <div class="entry-list">
              <% posts.forEach(function(post, index) { %>
                  <div class="entry-summary">
                      <div class="entry-header">
                          <span class="entry-date"><%= post.date %> - <%= post.time %></span>
                      </div>
                      <div class="entry-preview">
                          <p><%= post.entry.slice(0, 100) %>...</p>
                          <a href="/entry/<%= index %>" class="view-link">Read full entry</a>
                      </div>
                  </div>
              <% }) %>
          </div>
      </div>
  </body>
  </html>
  